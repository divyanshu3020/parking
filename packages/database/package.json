{
  "name": "@repo/database",
  "module": "index.ts",
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "bunx prisma generate",
    "push": "bunx prisma db push",
    "build": "bun run generate",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'database: no lint configured'"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@prisma/client": "^6.4.0",
    "prisma": "^6.4.0"
  }
}
